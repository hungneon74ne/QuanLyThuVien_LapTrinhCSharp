@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Quản trị hệ thống</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: #f8f9fa;
            min-height: 100vh;
            font-family: 'Roboto', sans-serif;
        }
        .sidebar-card {
            border-radius: 12px;
            overflow: hidden;
            z-index: 1000;
            position: relative;
        }
        .sidebar-header {
            background: #333;
            color: white;
            border-bottom: 2.5px solid #f1c40f;
        }
        .list-group-item {
            border: none;
            padding: 12px 20px;
            cursor: pointer;
            transition: all 0.2s ease;
        }
        .list-group-item:hover {
            background: rgba(241, 196, 15, 0.08);
        }
        .list-group-item a {
            display: block;
            width: 100%;
            height: 100%;
        }
        .list-group-item-active-gold {
            background: rgba(241, 196, 15, 0.12) !important;
            border-left: 4px solid #f1c40f !important;
        }
        .list-group-item-active-gold a {
            color: #f1c40f !important;
            font-weight: 700 !important;
        }
        .sidebar-submenu {
            padding-left: 15px;
        }
        .sidebar-divider {
            font-size: 11px;
            text-transform: uppercase;
            font-weight: 700;
            color: #6c757d;
            padding: 8px 20px;
            pointer-events: none;
        }
        .content-card {
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            overflow: hidden;
        }
        .content-header {
            background: #333;
            color: white;
            border-bottom: 2.5px solid #f1c40f;
            border-radius: 12px 12px 0 0;
        }
        .table thead th {
            background: #f8f9fa;
            border-bottom: 2px solid #dee2e6;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 12px;
        }
        .btn-primary {
            background: #333;
            border-color: #333;
            color: white;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            background: #222;
            border-color: #222;
            color: #f1c40f;
        }
        .btn-premium-gold {
            background: transparent !important;
            border: 1.5px solid #f1c40f !important;
            color: #f1c40f !important;
            font-weight: 600 !important;
            transition: all 0.3s ease !important;
        }
        .btn-premium-gold:hover {
            background: #f1c40f !important;
            color: #333 !important;
            box-shadow: 0 4px 15px rgba(241, 196, 15, 0.2) !important;
        }
        .btn-success {
            background: #28a745;
            border-color: #28a745;
        }
        .btn-danger {
            background: #dc3545;
            border-color: #dc3545;
        }
        .badge-warning {
            background: #ffc107;
            color: #212529;
        }
        .badge-danger {
            background: #dc3545;
        }
        .badge-success {
            background: #28a745;
        }
    </style>
</head>
<body>
    <div class="container-fluid py-4">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 mb-4">
                <div class="card sidebar-card border-0 shadow-sm">
                    <div class="card-header sidebar-header py-3">
                        <h5 class="mb-0 fw-bold"><i class="fas fa-shield-alt me-2"></i>Menu Admin</h5>
                    </div>
                    <div class="card-body p-0">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item @(ViewBag.ActiveMenu == "Index" ? "list-group-item-active-gold" : "")">
                                <a href="/Admin/Index" class="text-decoration-none @(ViewBag.ActiveMenu == "Index" ? "" : "text-secondary")">
                                    <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                                </a>
                            </li>
                            <li class="sidebar-divider">Quản lý hệ thống</li>
                            <li class="list-group-item @(ViewBag.ActiveMenu == "QuanLyNguoiDung" ? "list-group-item-active-gold" : "")">
                                <a href="/Admin/QuanLyNguoiDung" class="text-decoration-none @(ViewBag.ActiveMenu == "QuanLyNguoiDung" ? "" : "text-secondary")">
                                    <i class="fas fa-users-cog me-2"></i>Tài khoản hệ thống
                                </a>
                            </li>
                            <li class="list-group-item @(ViewBag.ActiveMenu == "QuyDinhThuVien" ? "list-group-item-active-gold" : "")">
                                <a href="/Admin/QuyDinhThuVien" class="text-decoration-none @(ViewBag.ActiveMenu == "QuyDinhThuVien" ? "" : "text-secondary")">
                                    <i class="fas fa-gavel me-2"></i>Quy định thư viện
                                </a>
                            </li>
                            <li class="sidebar-divider"></li>
                            <li class="list-group-item">
                                <a href="/Auth/Logout" class="text-decoration-none text-danger fw-bold">
                                    <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-9">
                @RenderBody()
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
